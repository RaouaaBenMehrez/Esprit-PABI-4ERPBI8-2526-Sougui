version: "3.9"

services:
  api:
    build: .
    container_name: sales-agent-api
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - chroma_data:/app/chroma_db
    restart: unless-stopped

volumes:
  chroma_data:
